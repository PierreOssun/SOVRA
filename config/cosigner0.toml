party_id = 0
bind_addr = "127.0.0.1:4100"
data_dir = "data/cosigner0"
relay_url = "wss://127.0.0.1:3100/env"
policy_path = "config/policy0.toml"
# mTLS material (seeded by `cargo xtask certs`); missing files abort startup
tls_ca_path = "certs/ca.cert.pem"
tls_cert_path = "certs/cosigner0.cert.pem"
tls_key_path = "certs/cosigner0.key.pem"
