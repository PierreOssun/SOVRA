# Copy beside cloud.yml as .env (sovra init does this) and fill in.
# THIS host's tailscale IP — goes into its cert SAN (sovra certs csr):
MY_TS_IP=
# The Mac (orchestrator/relay) tailscale IP:
MAC_TS_IP=
SOVRA_PARTICIPANTS=
SOVRA_CERTS_DIR=./certs
SOVRA_CONFIG_DIR=./config
SOVRA_TAG=latest
